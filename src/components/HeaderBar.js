import '../App.css';
import XButton from './XButton';

function HeaderBar() {
    return (
        <div className="Header-bar">
            <XButton></XButton>
        </div>
    );
}

export default HeaderBar;